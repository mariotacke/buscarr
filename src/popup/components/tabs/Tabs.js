import React, { Component } from 'react';
import './style.css';

const style = {
  backgroundColor: 'red',
};

class Tabs extends Component {
  render () {
    return (
      <div style={style}>
        {/* Tabs */}
      </div>
    );
  }
}

export default Tabs;